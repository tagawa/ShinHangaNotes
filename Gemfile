source "https://rubygems.org"

# Production builds via GitHub Actions (actions/jekyll-build-pages@v1).
# Locally we use Jekyll 4 which has better Ruby 3.x compatibility.
gem "jekyll", "~> 4.3"
gem "jekyll-sitemap"
gem "webrick" # removed from Ruby 3.x stdlib
